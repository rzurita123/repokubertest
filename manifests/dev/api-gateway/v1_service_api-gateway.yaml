apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-gateway
    environment: dev
    version: v1
  name: api-gateway
  namespace: dev
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: api-gateway
  type: ClusterIP
