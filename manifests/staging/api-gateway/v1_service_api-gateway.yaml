apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-gateway
    environment: staging
    version: v1
  name: api-gateway
  namespace: staging
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: api-gateway
  type: ClusterIP
